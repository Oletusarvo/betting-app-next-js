'use client';
type NumberAccumulatorProps = {
  fromValue: number;

  /**The final number into which to accumulate to.*/
  toValue: number;
};

export function NumberAccumulator({ fromValue, toValue }: NumberAccumulatorProps) {
  return null;
}
